/**
 * Gluestack UI 配置
 *
 * 注意：SafeAreaView 警告来自 Gluestack UI 库内部（@gluestack-ui/config）
 * 这不影响功能，因为我们的代码已正确使用 SafeAreaProvider
 * 等待 Gluestack UI 库更新以修复此警告
 */

import { config } from '@gluestack-ui/config';

// 导出默认配置
export { config };
